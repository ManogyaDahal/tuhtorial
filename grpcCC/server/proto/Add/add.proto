syntax = "proto3";
package add; 

option go_package = "./proto/gen/Add;gen_add";

service AddNumberService{ 
  rpc AddNumber(AddNumberRequest) returns (AddNumberResponse);
}

message AddNumberRequest {
  repeated int64 InputNum = 1;
}

message AddNumberResponse { 
  int64 Sum = 1; 
}
